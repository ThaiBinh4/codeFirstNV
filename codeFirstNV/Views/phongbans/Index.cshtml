@model IEnumerable<codeFirstNV.Models.phongban>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Namephongban)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.idcongty)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Namephongban)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.idcongty)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Idphongban">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Idphongban">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Idphongban">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
